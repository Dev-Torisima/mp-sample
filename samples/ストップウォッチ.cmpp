//ストップウォッチ（時間値）のサンプル

//開始の時間値
整数 開始 = 時間値の取得()

//測りたい処理
読み込む(10) //ここでは入力があるまでの時間を測ります
//待つ(1000)

//経過した時間（単位はミリ秒）は ( 今の時間値 - 開始の時間値 ) で表せます
書き込む(整数をテキストにする(時間値の取得() - 開始))

//詳しく：このサンプルで求める時間は ( 測りたい処理の時間 + 「時間値の取得」関数の呼び出し時間 ) になってしまいます

読み込む(1)

//「時間値の取得」関数は ”とある時間からの経過時間（単位はミリ秒）を取得する関数” です
