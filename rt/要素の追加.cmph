//ここでは「文字」型で実装
関数 要素の追加(文字［］ 配列、 char 対象、 整数 場所) ： 文字［］
    変数 結果 = 文字［］(要素数(配列) + 1)
    整数 i = 0
    整数 j = 0

    ループ (要素数(配列) + 1)
        もし (i == 場所)
            結果[i] = 対象
        ほか
            結果[i] = 配列[j]
            j += 1
        おわり もし

        i += 1
    おわり ループ

    返す 結果
おわり 関数
