//ここでは「文字」型で実装
関数 配列の切り取り(文字［］ array, 整数 start, 整数 size) ： 文字［］
    文字［］ 結果 = []
    もし (size > 0)
        結果 = 文字［］（size）
        整数 i = 0
        ループ(size)
            結果[i] = array[start + i]
            i += 1
        おわり ループ
    おわり もし

    返す 結果
おわり 関数
