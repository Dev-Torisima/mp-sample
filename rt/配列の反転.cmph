//ここでは「文字」型で実装
関数 配列の反転(文字［］ array) ： 文字［］
    var 結果 = 文字［］(len(array))
    整数 i = 0
    整数 j = len(array) - 1
    ループ(len(array))
        結果[i] = array[j]
        i += 1
        j -= 1
    おわり ループ

    返す 結果
おわり 関数
